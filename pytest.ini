[pytest]
testpaths = tests
python_files = test_*.py
markers =
    smoke: Fast portal smoke coverage for deployed app surfaces.
    unit: Isolated unit tests with mocked dependencies.
    integration: Tests that require live services, seeded data, or external dependencies.
